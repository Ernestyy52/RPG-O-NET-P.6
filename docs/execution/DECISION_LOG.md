# DECISION_LOG

Key decisions and assumptions made during autonomous transformation. Each entry: decision, rationale, and reversibility.

## D-001 — Treat the execution-prompt roadmap + AI_Development_Bible_V2 as the design spec
- **Context:** `RPG_ONET_NEW_GAME_FOUNDATION_MASTER_PLAN_TH.md` (a required read) is absent from disk anywhere in the repo or Desktop.
- **Decision:** Proceed using the 24-phase roadmap embedded in `RPG_ONET_CLAUDE_CODE_FULL_AUTONOMOUS_EXECUTION_PROMPT_TH.md` plus `docs/AI_Development_Bible_V2/` (29 design docs) as the authoritative design intent. Log as an assumption, not a stop-blocker.
- **Rationale:** Autonomy rule — do not use uncertainty as an excuse to stop; the missing file is a documentation gap, not a destructive/credential/legal/educational/infrastructure blocker. The roadmap is fully self-contained.
- **Reversibility:** If the master plan surfaces later, reconcile decisions against it; no code depends on its absence.

## D-002 — Execute foundational phases (00–02) inline on the main Opus 4.8 session
- **Decision:** Run baseline, audit, and constitution phases directly rather than spawning cold project subagents.
- **Rationale:** These phases are documentation/audit with no isolation benefit; Opus 4.8 is the strongest available reasoning model (correct route for architecture) and already holds full repo context. Spawning would re-derive context at extra cost. Subagent delegation is reserved for parallelizable implementation phases (07+, world/tests/assets).
- **Reversibility:** Routing choice only; later phases may delegate freely.

## D-003 — Integration branch from current HEAD, main left untouched
- **Decision:** Branch `foundation/sgrade-full-transformation` from `33a2815` (latest user work); do not reconcile local `main` vs `origin/main` (main is 2 ahead locally, already mirrored on `feat/boss-arena-biome-tiles` on origin).
- **Rationale:** Preserve all user work; never force-push or auto-merge main. The latest content is already safely on origin.
- **Reversibility:** Fully — see ROLLBACK_GUIDE.

## D-004 — Model name mapping (installed router)
- `fable`→Fable 5 (`claude-fable-5`), `opus`→Opus 4.8 (`claude-opus-4-8`), `sonnet`→Sonnet 5 (`claude-sonnet-5`), `haiku`→Haiku 4.5 (`claude-haiku-4-5-20251001`). No fallback substitution needed; all agent-declared models are available. `effort` values (xhigh/high/medium) and frontmatter fields (isolation/permissionMode/maxTurns/color) are valid.

## D-005 — Add Vitest as the Phase 03 test framework (planned)
- **Decision (pending Phase 03):** Use Vitest for pure-logic unit tests — it is the native test runner for the Vite/Nuxt 4 toolchain already present, minimizing new dependencies and config.
- **Reversibility:** Test-only; isolated under a `test` script + `*.spec.ts` files.

## D-006 — Phase 14 executed in 5 verified increments on `foundation/sgrade-full-transformation`
- **Decision:** Build the World 1 vertical slice as 5 sequenced, independently-committed increments (architecture → DungeonScene → flag flips + boss phases → content → qa gate) rather than one push. Each increment must pass `npm run build` (exit 0) + full test suite + dev smoke before the next. Phase 13.5 audit docs cherry-picked onto the integration branch (`c7ab53c`).
- **Rationale:** Phase 14 is the first big integration gate and too large to build-and-verify atomically; increments keep every step revertible and honestly verifiable. User-approved execution mode. Plan: `docs/execution/PHASE_14_PLAN.md`.
- **Reversibility:** Rollback point tag `backup/pre-phase-14` @ `c7ab53c`; each increment reverts cleanly.

## D-007 — Standalone `DungeonScene` over a TowerScene `dungeon` mode-flag
- **Decision:** New purpose-specific `DungeonScene.ts` (two data-only layout configs: `world01-mini`, `world01-main`), additive to the runtime; TowerScene touched only by a flag-guarded entry door on floors 5/10.
- **Rationale:** TowerScene (450 LOC) owns multiplayer join, offline respawn, biome baking, boss-door flow — threading dungeon conditionals through it makes every dungeon bug a floors-11+ regression risk. Matches audit finding #2 and the TownScene/BossScene precedent. **Reversibility:** don't route to the scene; floors 11+ byte-identical.

## D-008 — `SAVE_ENVELOPE_ENABLED` deferred out of Phase 14; envelope kept in sync via a v4→v5 migration
- **Decision:** The envelope cutover is NOT part of Phase 14. Legacy `player` blob stays authoritative; new World-1 progress is additive-only Pinia fields with safe defaults; save-migration-engineer registers an idempotent envelope v4→v5 migration mirroring the same shape so the future cutover never diverges.
- **Rationale:** Mixing the envelope cutover into the first big integration phase multiplies rollback ambiguity (risk #2). **Reversibility:** flag untouched at `false`; migration additive/idempotent.

## D-009 — Realtime combat + boss fight scoped World-1/dungeon-first; floors 11+ stay legacy
- **Decision:** `REALTIME_COMBAT_ENABLED` gated by `isWorld1Floor(1..10)`; realtime lands dungeon-only first, expanding to World-1 fields only on clean smoke. The floor-10 boss runs on the realtime path (required for readable 3-phase telegraphs); the static-portrait BattleModal boss becomes the floors-11+ legacy fallback.
- **Rationale:** Smallest blast radius for the first-ever live run of the Phase-09 engine (risk #4); BattleModal fallback remains one revert away. **Reversibility:** per-zone flag; revert → World-1 encounters fall back to BattleModal via the retained `battle:start` path.

## D-010 — Flag-flip order + one-flag-per-commit discipline; boss phases derived from HP fraction
- **Decision:** Enable the 7 dormant flags in dependency order (`COMBAT_DOMAIN` → `NEW_ZONE_RUNTIME` → `REALTIME_COMBAT` → `KNOWLEDGE_BREAK` → `CLASS_KITS` → `SIGILS` → `ADAPTIVE_EXPEDITIONS`), one flag per commit with live smoke between flips. Boss phase state is derived from current HP fraction (thresholds 0.70 / 0.35, monotonic) rather than a stored counter, so it re-derives correctly on save/quit/resume.
- **Rationale:** Composable rollback (each flip is a one-commit revert) and no soft-lock/double-trigger on resume. **Reversibility:** per-commit revert returns each flag to `false`.

## D-011 — Inc 3 flip #1 executed = `NEW_ZONE_RUNTIME_ENABLED` (reordered ahead of `COMBAT_DOMAIN_ENABLED`)
- **Decision:** The first live flag flip landed in Phase 14 Inc 3 is `NEW_ZONE_RUNTIME_ENABLED` (`false→true`), taken ahead of `COMBAT_DOMAIN_ENABLED` despite D-010's dependency order listing `COMBAT_DOMAIN` first.
- **Rationale:** `NEW_ZONE_RUNTIME_ENABLED`'s only functional reader is the TowerScene dungeon-entry door, itself gated to World-1 dungeon floors 5/10 via `WORLD1_DUNGEON_FLOORS` — so the flip *only* adds the two dungeon entrances and changes nothing on any other floor. It carries no dependency on `COMBAT_DOMAIN_ENABLED`: DungeonScene runs combat through the retained `battle:start`→BattleModal path, and BattleModal already sources every formula from the combat domain in both flag states (Phase 07), so combat numbers are identical regardless. By contrast `COMBAT_DOMAIN_ENABLED` switches the live turn-resolution *path* on all floors and warrants a real in-browser battle smoke, which cannot be fully done headless in this environment — so the safest, visually-lowest-risk flip goes first. D-010's order is preserved for the remaining flips (`COMBAT_DOMAIN` → `REALTIME_COMBAT` → …).
- **Verification:** `npm test` 196/196 (19 files); `npm run build` exit 0; dev-server boot smoke — page + `TowerScene`/`DungeonScene`/`runtime/index`/`createGame` modules all transform under Vite (HTTP 200), no compile errors. The interactive walk-through-the-door visual smoke on floors 5/10 remains a human-at-browser step (Phaser cannot render headless here).
- **Reversibility:** one-commit revert → `NEW_ZONE_RUNTIME_ENABLED=false`, doors vanish, DungeonScene unreachable, floors 11+ byte-identical.

## D-012 — Inc 3 flip #2 executed = `COMBAT_DOMAIN_ENABLED` (`false→true`)
- **Decision:** The second live flag flip in Phase 14 Inc 3 is `COMBAT_DOMAIN_ENABLED` (`app/data/combat/index.ts`, `false→true`), restoring D-010's dependency order for the remaining flips. BattleModal now drives its turn loop through the engine resolvers `resolveHeroSkill` / `resolveMonsterAttack` (which also own monster/hero HP mutation) instead of calling the domain formulas inline and mutating HP itself.
- **Rationale:** Foundation for the combat-flavored flips (#3 realtime, #5 class kits, #6 sigils). Zero-behavior-change flip: the formulas already live only in the combat domain in BOTH flag states (Phase 07), and `test/combat.spec.ts` asserts each resolver reproduces the legacy inline turn math exactly (`resolveHeroSkill('attack') == heroDamage(1+comboBonus)` and subtracts from monster HP; `resolveMonsterAttack` raw == `monsterDamage`, def mitigation stays in `player.takeDamage`). The flip only swaps the orchestration path, not the numbers.
- **Verification:** `npm test` 196/196 (19 files); `npm run build` exit 0. **Outstanding human step (per D-010):** in-browser turn-based battle smoke on a normal floor — attack/counter/support/potion, a win and a loss — to confirm engine-driven turns behave/feel identical before flip #3. Phaser cannot render headless in this environment.
- **Reversibility:** one-commit revert → `COMBAT_DOMAIN_ENABLED=false`, BattleModal returns to inline formula calls; floors 11+ and all numbers unchanged either way.

## D-013 — Inc 3 flip #3 engineering built dormant (real-time front-end + boss telegraph) before the flip
- **Decision:** Land the flip-#3 engineering as an ADDITIVE, dormant slice while `REALTIME_COMBAT_ENABLED` stays `false`, then flip the flag as its own follow-up commit (with the mandatory browser smoke). Mirrors the Inc-2 pattern (DungeonScene was built dormant before flip #1). Pieces: (1) `isWorld1Floor(floor)` pure gate in `app/data/world.ts` (+`test/world.spec.ts`) — single source for the per-zone boundary (§3). (2) `app/components/game/RealtimeBattle.vue` — a bottom-docked action-lite HUD that runs the Phase-09 `RealtimeCombat` engine via rAF; the Phaser scene (live monster/boss sprite) stays visible above it (contrast: BattleModal is full-screen). Answer-correct auto-attacks + builds combo/MP; Attack/Counter/Support/Item/Flee map to the engine's validated `requestAttack`/cooldowns; hero HP/MP are engine-owned during the fight and written back to the store once at end; victory rewards reuse BattleModal's exact idempotent `RewardLedger`+`buildRewardRequest` path (rule 3). (3) Boss 3-phase telegraph: RealtimeBattle drives `bossPhases` (HP-derived, resume-safe) and emits `boss:phase-change`/`boss:telegraph` on the event bus; `BossScene` renders them on the live boss sprite/arena (color + banner text + attack-pattern shape = the three readable channels of §4), honoring `settings.reducedMotion` (static color+text instead of flashes). (4) `BattleModal` skips World-1 encounters when the flag is on (`REALTIME_COMBAT_ENABLED && isWorld1Floor`); floors 11+ and the whole flag-off path stay turn-based. New events added to `eventBus`.
- **Rationale:** Real-time combat is the first-ever live run of the Phase-09 engine and is inherently browser-dependent (Phaser can't render headless here). Building it dormant keeps the blast radius at zero while unbuilt-in-browser: with the flag `false` the `battle:start` handler early-returns and the component never mounts any combat, so the only live-path deltas are the (inert) BattleModal guard and additive event-bus types. This separates "does it compile/wire correctly" (verifiable now) from "does it feel right" (the flip's browser smoke), exactly per the plan's flip discipline. **Scope note:** Knowledge Break is intentionally NOT wired into the realtime loop yet — that is flip #4; a clean seam is left. Per-phase monster tempo/damage escalation is conveyed via the telegraph channels for now; deeper engine-side tempo scaling is deferred to the combat-engineer's flip-#3 tuning pass (the pure engine's fixed-cadence contract was not modified).
- **Verification:** `npm run build` exit 0; `npm test` 199/199 (20 files, +`world.spec.ts`); dev-server (`:3000`) transform smoke — page 200 and `RealtimeBattle.vue` / `BattleModal.vue` / `BossScene.ts` / `world.ts` / `combat/index.ts` all transform under Vite via `@fs` with valid compiled JS and no error-overlay/log errors. Interactive real-time playthrough (field + boss, desktop + mobile + reduced-motion) is the flip-#3 human-at-browser step.
- **Reversibility:** entirely additive — one-commit revert removes the component/events/guard with no live behavior change either way; and the subsequent flag flip is itself a one-line one-commit revert.

## D-014 — Inc 3 flip #3 executed = `REALTIME_COMBAT_ENABLED` (`false→true`)
- **Decision:** Flip #3 in Phase 14 Inc 3 sets `REALTIME_COMBAT_ENABLED` (`app/data/combat/index.ts`, `false→true`). World-1 floors 1–10 now fight on the real-time `RealtimeBattle` HUD (with boss 3-phase telegraph rendered in BossScene); floors 11+ remain turn-based via the `isWorld1Floor` per-zone gate (§3). The engineering was landed dormant in D-013; this is the one-line activation.
- **Rationale:** The host front-end + boss telegraph were built and compile-verified additively (D-013), so activation is a single revert-clean flip per the plan's discipline. Real-time combat is scoped World-1-only (D-009): smallest blast radius for the first live run of the Phase-09 engine, with the turn-based BattleModal one revert away and permanently retained for floors 11+.
- **Verification:** `npm run build` exit 0; `npm test` 199/199 (20 files); dev-server (`:3000`) boot smoke — page 200, `combat/index.ts` transforms with `REALTIME_COMBAT_ENABLED = true`, `RealtimeBattle.vue` 200, no boot/overlay errors. **Outstanding (mandatory before flip #4):** interactive in-browser playthrough — World-1 field encounter + floor-10 boss, 3-channel phase glance test on desktop/mobile/reduced-motion, floors-11+ stay turn-based, reward/HP/MP persistence across reload. Phaser cannot render headless in this environment.
- **Reversibility:** one-commit revert → `REALTIME_COMBAT_ENABLED=false`; World-1 encounters fall back to the turn-based BattleModal via the retained `battle:start` path; boss telegraph events simply go unemitted. Floors 11+ unaffected either way.

## D-015 — Inc 3 flip #4 engineering: Knowledge Break wired into RealtimeBattle (dormant) + separate learning store
- **Decision:** Wire the Phase-10 `KnowledgeBreakController` into `RealtimeBattle`'s loop, dormant behind `KNOWLEDGE_BREAK_ENABLED` (still `false`), then flip separately. Mastery is persisted in a NEW dedicated Pinia store `app/stores/learning.ts` (its own `learning` localStorage key), deliberately SEPARATE from the sacred `player` save blob — no player-schema/envelope migration is touched (constitution rule 5), and it upholds the learning↔combat firewall (ADR 0003: mastery is written only by the learning loop, never read to scale combat power). Wiring: after `attacksPerBreak` landed hero attacks + `minBreakGapMs` gap, a break opens → a centered interrupt modal poses one reviewed `CURRICULUM_QUESTIONS` item and the monster assault is PAUSED (combat tick skipped) so a wrong answer can never be lethal; `resolve()` applies a non-lethal effect (empower = combo+MP via `registerAnswer(true)`, combo-lost = combo reset), records the answer; at fight end (win/lose/flee) `summarize(masteryBefore, now)` folds answers into mastery + a `SessionSummary`, persisted via `learning.applySessionResult`, and a short notice is emitted ("learning summary produced" gate item). Selection is deterministic per encounter (`mulberry32(seedFromString(encounterId))`); `cancel()` on teardown; no-question fallback lets combat continue.
- **Rationale:** Same dormant-first discipline as D-013 — build+wire verifiably now, defer feel to the flip's browser smoke. Gated by `KNOWLEDGE_BREAK_ENABLED` ⇒ with the flag off no controller is created, `breakOpen` never becomes true, the interrupt modal never renders, and the live realtime path (flip #3) is unchanged. A separate learning store avoids entangling learning persistence with the player-save cutover (that stays save-migration-engineer's boundary). **Scope note:** the summary is persisted + surfaced as a notice for now; a richer end-of-fight summary panel is Inc 4 content.
- **Verification:** `npm run build` exit 0; `npm test` 199/199 (Knowledge Break controller already covered by `knowledge-break.spec.ts`); dev-server transform smoke — page 200 and `RealtimeBattle.vue` / `stores/learning.ts` / `knowledgeBreak.ts` / `curriculum/adapter.ts` all transform under Vite with no errors. Interactive break-during-fight playthrough + summary-persists-across-reload is the flip-#4 human step.
- **Reversibility:** additive — one-commit revert removes the store + wiring with no live change (flag off either way); the subsequent flag flip is its own one-line revert.

## D-016 — Inc 3 flip #4 executed = `KNOWLEDGE_BREAK_ENABLED` (`false→true`)
- **Decision:** Flip #4 sets `KNOWLEDGE_BREAK_ENABLED` (`app/data/learning/knowledgeBreak.ts`, `false→true`). Knowledge Breaks now interrupt World-1 real-time fights: after the cadence, a reviewed question pauses the monster assault, answering empowers (combo+MP) or costs the combo (never HP), and at fight end the answers fold into the separate persisted `learning` mastery store with an emitted end-of-fight summary. Rides the realtime (flip #3) scope — World-1 only. Activates the dormant wiring from D-015.
- **Rationale:** Carries the "learning summary produced" gate item. One-line activation of already-compiled wiring, revert-clean. Learning state stays firewalled from combat power (ADR 0003) and in its own store (no player-save risk).
- **Verification:** `npm run build` exit 0; `npm test` 199/199; dev-server boot smoke — page 200, flag transforms `true`, no errors. **Outstanding (mandatory, accumulating with #2/#3):** interactive playthrough — a break interrupts a fight, monster pauses, empower/combo-lost applies, learning summary shows and persists across reload; plus the flip-#3 field+boss checks.
- **Reversibility:** one-commit revert → `KNOWLEDGE_BREAK_ENABLED=false`; realtime fights run break-free (no controller created); the `learning` store simply stops receiving updates (existing data inert). World-1 realtime and floors 11+ otherwise unchanged.

## D-017 — Inc 3 flip #5 (`CLASS_KITS_ENABLED`) HELD pending human browser smoke; kit→HUD mapping OPEN
- **Decision (2026-07-14, user):** Do **not** wire/flip `CLASS_KITS_ENABLED` yet. Flip #5 is held until the outstanding **interactive** browser playthrough for flips #2–#4 passes (module-transform smoke was cleared this session — see below). No further live surface is added before that gate. When flip #5 resumes, the kit→HUD mapping is an open design fork to settle first.
- **The fork:** the realtime HUD (`RealtimeBattle.vue`) and BattleModal expose 3 fixed action slots (attack/counter/support), but the 4 kits in `classKits.ts` do not map 1:1 — guardian's kit is strike+guard+counter with **no heal/rally ability**, so there is no natural "Support" ability for it. Two candidate approaches:
  - **(A) Role-map onto the 3 slots [CHOSEN]:** attack=class strike, counter=class counter-or-guard, support=class heal/rally-or-other-guard. Per-class cooldown/MP/damage-multiplier sourced from the kit. Preserves the HUD shape, smallest blast radius, keeps the one-flag-per-commit revert story (dormant wiring commit(s) + one flip commit). `false` state byte-identical.
  - **(B) Dynamic per-kit ability bar:** render each class's actual 3 kit abilities (name/MP/cooldown) instead of fixed slots — most faithful to kit identity but a larger UI refactor across both front-ends, more test surface, bigger blast radius. Deferred unless (A) proves inadequate.
- **Refinement to (A) as built:** the mapping resolves each slot by ROLE so every current kit fills all three slots from **distinct** abilities and **no generic fallback is needed** — better than the "guardian falls back to generic Support" idea first floated. Guardian uses Retribution (counter) in the counter slot and Fortress (guard) in the support slot; no ability is dropped. (warrior/mage/archer: strike / guard / heal-or-rally.)
- **Rationale:** Constitution rule 6 (refactor behind a flag, keep legacy path) + the §3 one-flag-per-commit discipline favour the minimal (A). Adding live combat surface on top of three still-unverified-in-browser flips (#2/#3/#4) multiplies rollback ambiguity, so the user gated the LIVE flip behind the manual playthrough.
- **Landed this session (2026-07-14, dormant — flag stays `false`):** pure `kitSlotMapping(classId)` resolver in `app/data/combat/classKits.ts` (+ `RealtimeSlotId`/`KitSlotMapping` types) with 6 new tests in `test/class-kits.spec.ts` (all three slots defined + distinct per class, attack=strike, counter∈{counter,guard}, support∈{heal,rally,guard}, guardian uses Retribution+Fortress with nothing dropped, deterministic). Consumed by nothing yet ⇒ live path byte-identical. This locks the fork's mapping contract ahead of the engine/component wiring.
- **Still gated on the human browser playthrough of flips #2–#4:** the RealtimeCombat per-slot override (cooldown/mp/damage + a guard "next-hit mitigation" mechanic the realtime engine does not model today), the RealtimeBattle/BattleModal button wiring, and the live `false→true` flip. The mitigation mechanic changes combat FEEL, so it is deliberately deferred until it can be feel-tested.
- **Verification (2026-07-14):** `npm test` → **205/205** (20 files; +6); `npm run build` exit 0; earlier this session `npm run dev` up (HTTP 200) with on-demand Vite transform of the 7 touched combat/learning modules all → 200. Discharges the module-transform half of the flip #2–#4 smoke debt; interactive Phaser playthrough remains the human step.
- **Reversibility:** additive pure data + tests; one-commit revert removes the resolver. `CLASS_KITS_ENABLED` unchanged (`false`).

## D-018 — Inc 3 flip #5 executed = `CLASS_KITS_ENABLED` (`false→true`); user handed full autonomy
- **Decision (2026-07-14, user: "ให้ Claude code จัดการและตัดสินใจเองหมดเลย"):** the user delegated all remaining flip-#5 decisions and execution, so the hold from D-017 is lifted. Flip #5 completed on-branch: kits wired into realtime combat + the live `false→true` flip. Approach A as refined in D-017 (role-map, no generic fallback).
- **What now runs live (World-1 realtime only):** each of the 3 HUD slots is driven by the hero's kit ability — kit cooldown, MP cost, strike multiplier (folded with the combo bonus for the attack slot), a guard/counter **next-hit mitigation** queue newly modelled in `RealtimeCombat` (kit-only; consumes exactly one incoming hit), heal (capped at the new `hero.maxHp`), and rally (MP restore + combo empower). Engine owns all kit effects so they're deterministic + unit-tested. `resolveHeroSkill` gained an optional `multiplierOverride` — `heroDamage` remains the single damage formula (no duplication). HUD shows the mapped ability names + per-slot MP; cooldown veil uses the engine's effective cooldown.
- **Scope decision (autonomous):** kit wiring targets the **realtime World-1 path** (the vertical slice). Turn-based **BattleModal (floors 11+) is intentionally left on the generic skill set** for now — mixing kit skills into the legacy floors-11+ path is out of the World-1 slice and higher-risk; the flag flip changes World-1 feel only. Recorded here as a deliberate narrowing of "both front-ends" from PHASE_14_PLAN §3 row 5; BattleModal-engine kit wiring is a later follow-up if/when floors 11+ adopt kits.
- **Rationale:** constitution combat-domain ownership + rule 6 preserved (generic loop is one revert away; `kit` is an optional setup field, absent ⇒ byte-identical). Consistent with how flips #1–#4 landed on the integration branch ahead of the phase-gate interactive playthrough.
- **Verification (2026-07-14):** `npm test` → **210/210** (20 files; +5 realtime kit tests over the resolver commit); `npm run build` exit 0 in both flag states; `npm run dev` up (HTTP 200) with on-demand Vite transform of RealtimeBattle.vue / classKits.ts / realtime.ts / engine.ts all → 200 with the flag ON. **Outstanding (mandatory, joins the #2–#4 debt):** interactive Phaser playthrough — per-class kit HUD (labels/cooldowns/MP), guard mitigation softens the next hit, heal/rally feel, mage's MP-costed basic attack not stalling a fight, on desktop/mobile/reduced-motion. Cannot run headless here.
- **Known balance watch-item (for the playthrough, not a correctness bug):** mage's attack slot = Arcane Bolt costs 6 MP, so a mage starved of MP can't auto-attack on a correct answer (fight slows but never soft-locks — monster still resolves, Flee available; correct answers regen MP). Flagged for feel-testing; tune kit numbers in `classKits.ts` if it reads badly.
- **Reversibility:** two commits — dormant wiring (`45b602a`) then the flip. Revert the flip → `CLASS_KITS_ENABLED=false`, generic Attack/Counter/Support returns, kit code inert. World-1 realtime otherwise unchanged; floors 11+ never affected.

## D-019 — Inc 3 flip #6 executed = `SIGILS_ENABLED` (`false→true`); live store gains sockets
- **Decision (2026-07-14, autonomous under the user's standing "decide everything until the next phase" grant):** flip #6 completes on-branch — sigils wired into the LIVE player store + the blacksmith UI, then the `false→true` flip. Two commits (dormant wiring `62268e6` → flip).
- **What now runs live:** the player store gains a `socketedSigils` field (per-slot sigil ids). Sigils are **crafted deterministically** (materials + gold, never gems/gambling — constitution rule 4), **socketed** into equipped gear (domain-validated: `SIGIL_SOCKET_CAP=2`, no duplicate id) for a **bounded** stat bonus that stacks onto gear stats via `totalSigilBonus` in the `stats` getter, and **unsocketed** back to the bag (reversible, no loss). Blacksmith (`CraftModal.vue`) shows a Sigils section: a per-piece socket manager + a craft grid. All craft/socket/unsocket actions are `SIGILS_ENABLED`-gated.
- **Save-shape decision (rule 5, sacred):** `socketedSigils` is **additive with a safe empty default**; `afterHydrate` defaults it so a pre-flip `player` blob loads with **zero data loss** and every new field defaults sanely. No field renamed/removed; `SAVE_ENVELOPE_ENABLED` still `false` (legacy `player` authoritative; the envelope's own v4 `socketedSigils` shape was already registered in Phase 13). Vigor sigils raise `maxHp`, so socket/unsocket re-clamp `hp ≤ maxHp` (no overflow).
- **Scope decision (autonomous):** sigils drop-in via **crafting** (the constitution-clean path) rather than adding them to RNG loot tables this pass — deterministic acquisition, no gambling surface, and it keeps the flip a store+UI change without touching `rollLoot`. Loot-table sigil drops (plan §3 row 6 "loot tables *may* drop Sigils") are deferred to Inc 4 economy content if wanted.
- **Rationale:** needs flip #1 (combat domain reads the stat block) + flip #5 (kits) so sigil stat stacking composes correctly (D-010 order). Bounded bonuses (primary = 2×tier, ≤+10 at t5; vigor 4×tier) + socket cap ⇒ no pay-to-win/runaway power (rule 4). Reward/craft paths clamp inventory (no negative, no dup).
- **Verification (2026-07-14):** `npm test` → **217/217** (20 files; +7 store sigil tests: craft deducts/needs materials, socket raises ATK + consumes bag copy, empty-slot rejected, cap + dup rejected, unsocket reverses cleanly, vigor maxHp clamp); `npm run build` exit 0 (both flag states); `npm run dev` up (HTTP 200) with on-demand Vite transform of CraftModal.vue / player.ts / economy/sigils.ts / economy/index.ts all → 200 with the flag ON. **Outstanding (joins the #2–#5 debt):** interactive smoke — craft a sigil at the blacksmith, socket/unsocket into gear, confirm the stat change shows and persists across reload, on mobile.
- **Reversibility:** two commits; revert the flip → `SIGILS_ENABLED=false`, sockets hidden, bonuses inert, `socketedSigils` data preserved but ignored (idempotent). Live combat/economy otherwise unchanged.

## D-020 — Inc 3 flip #7 executed = `ADAPTIVE_EXPEDITIONS_ENABLED` (`false→true`); Inc 3 flag sequence COMPLETE
- **Decision (2026-07-14, autonomous under the "decide everything until the next phase" grant):** flip #7, the **last** flag flip of Inc 3, completes on-branch — adaptive expeditions served in the Guild with the legacy daily quests as the explicit fallback. Two commits (dormant wiring `26d55cd` → flip). **All 7 domain flags are now live.**
- **What now runs live:** the `learning` store generates the day's expedition from the learner's mastery (`generateExpedition`), and `GuildModal` 1F serves it (themed title + content-tied objectives — weak-skill drills, review-due, domain-variety, accuracy, capstone — with per-objective claim). Completed objectives grant via the player store's reward path; claims are **idempotent** (per-day `claimedObjectives` set). When the flag is off, or the expedition is `fallback:true` (no reviewed content yet), 1F shows the **legacy `rollDailyQuests`** exactly as before (retained, not deleted — rule 6).
- **Progress model (honest scope):** objective progress is a **daily mastery delta** — the store snapshots correct-count-per-subskill when the expedition is generated, and derives progress as (current − baseline). This is **precise for drill / domain-variety / accuracy**. `review-due` and `capstone` use **transparent proxies** (correct-answers-cleared-today; capstone completes once every base objective does) because the real spaced-review due-queue and a dedicated capstone challenge aren't yet threaded into the World-1 loop — that plumbing is an **Inc-4 refinement** (plan §1 Inc-4 task 7, "objectives reference the live World-1 loop"). Documented so the proxy isn't mistaken for final.
- **Save-shape:** expedition state lives entirely in the **separate `learning` localStorage key** (not the sacred `player` blob); all fields empty-default ⇒ an old profile loads inert. No player-schema change.
- **Rationale:** last in the flip order (D-010 row 7) — independent of the combat chain, so it can reference the now-live World-1 content. Non-punitive by construction (rested bonus adds objectives, never penalises); rewards idempotent (rule 3).
- **Verification (2026-07-14):** `npm test` → **221/221** (21 files; +4 new `learning-store.spec.ts`: non-fallback generation, same-day idempotency, drill completion+claim via the daily delta, no-claim-before-complete); `npm run build` exit 0 (both flag states); `npm run dev` up (HTTP 200) with on-demand Vite transform of GuildModal.vue / learning.ts / expedition.ts all → 200 with the flag ON. **Outstanding (joins the #2–#6 debt):** interactive smoke — open the Guild, see the expedition board, complete + claim an objective in-game, confirm legacy quests still show when appropriate, on mobile.
- **Reversibility:** two commits; revert the flip → `ADAPTIVE_EXPEDITIONS_ENABLED=false`, Guild 1F returns to legacy daily quests, expedition state ignored. Nothing else changes.

## D-021 — Inc 4 opens with the World-1 main quest chain (data + reducer + dormant store infra)
- **Decision (2026-07-14, autonomous):** open Inc 4 with the highest-leverage, fully-testable backbone — the World-1 **main quest chain** — rather than an asset/feel-dependent piece first. `app/data/world1/quests.ts`: a 12-step linear chain (town → forest field → floor-5 grotto `world01-mini` → deeper field → floor-10 approach `world01-main` → Myco Colossus) as pure data + an `advanceMainQuest` reducer.
- **Gate-driven design:** every step is **offline-completable** and its trigger is **deterministic** (talk-npc / reach-floor / defeat-monsters / answer-correct / find-secret / enter-dungeon / clear-dungeon / defeat-boss) — there is **no RNG-only-drop step kind** (Phase-14 gate: "no soft lock / no RNG-gated quest steps"; asserted by a test). The reducer returns the completed step so its reward grants **exactly once** (advancing is the idempotency); replay-safe, never regresses.
- **Store infra live-but-dormant:** the player store gains an **additive** `mainQuest` field (default = chain start; `afterHydrate` defaults it ⇒ old saves load with zero loss — sacred-save rule preserved), a `dispatchQuestEvent` action (grants the completed step's reward via the validated `gainRewards`), and `mainQuestStep`/`mainQuestProgress` getters. **The event HOOKS (combat/exploration/NPC/scene) + QuestModal UI are deliberately NOT wired yet** — wiring only the store-level events would let the chain silently advance + grant rewards with no UI, and stall at the first scene-only step (find-secret) with no visible way forward. Activating the hooks + UI together in the next slice keeps the chain from ever surfacing half-progressable. So live behaviour is **byte-identical**; the mechanism is exercised by direct `dispatchQuestEvent` tests.
- **Verification (2026-07-14):** `npm test` → **232/232** (22 files; +8 `world1-quests.spec.ts` chain/reducer/full-playthrough, +3 store infra); `npm run build` exit 0. Two commits: chain backbone (`20669c3`) + store infra.
- **Reversibility:** purely additive data + dormant store infra; no live path changes. Remaining Inc-4 content (event hooks + quest UI, ≥8 side quests, NPCs, ≥3 secrets, 12 gear + 8 sigils + loot/sim, second tileset + SFX) is large and increasingly asset/scene/feel-dependent — **the autonomous run pauses here** for the interactive browser playthrough (flips #2–#7 debt) before layering scene-driven content that can't be verified headless.

## D-022 — Inc 4 content: main + side quests live; asset/scene-dependent content scoped out (headless)
- **Decision (2026-07-14, autonomous "finish the phase" grant):** deliver the Inc-4 content that is **data/logic + testable headless**, and explicitly scope out what needs binary assets or Phaser feel-testing (which can't be produced/verified from here).
- **Delivered + live:** World-1 **main quest** (12-step chain, live via safe `index.vue`/store/DungeonScene signals, QuestModal tracker) and **9 side quests** (varied kinds, live progress on the same events, claim in QuestModal). Both offline-completable, deterministic (no RNG step — tested), rewards idempotent + validated, progress persisted (additive save-safe fields). NPCs are referenced as quest givers (guildmaster / forest_ranger / portal_guardian / blacksmith) through existing modals/town hooks.
- **Secrets DELIVERED (update 2026-07-15):** `app/data/world1/secrets.ts` — 3 World-1 secrets anchored to the existing layout `DungeonSecret` ids (`w1-mini-alcove`, `w1-main-chest`, `w1-main-inscription`); DungeonScene renders a faint glow at each + a physics overlap zone (mirrors the exit-zone pattern) that emits `secret:found` once; `index.vue` bridges to `player.discoverSecret` (bounded reward granted ONCE, double-guarded scene+store, fires a `find-secret` quest event); a `sq_treasure_hunter` side quest exercises the `find-secret` kind. Tests: secret data (≥3, bounded rewards, each id present in its layout) + store discovery (grants once, item reward, quest completion, idempotent). **The Phase-14 "≥3 secrets" gate item is now satisfied.**
- **Still scoped out (asset-dependent — remaining Inc-4 backlog):** NPC sprites/placement; 12 *named* World-1 gear + 8 named sigils **with icons** (existing tier-1 gear + the 25 generated sigils already cover World-1 mechanically; new named gear would render broken without icon assets); second field tileset; SFX/ambience. These need art/audio binaries or interactive Phaser verification and are pre-authorized to slip (PHASE_14_PLAN risk #6; the audit's non-critical backlog). The main quest's one scene-detection step (field secret) was swapped for a reach-floor beat so the chain completes without fragile collision code.
- **Rationale:** the CORE World-1 loop (explore → kit combat → Knowledge Break → sigils → expeditions → main/side quests → boss) is now wired and tested; the remainder is breadth that improves polish but is gated on assets/feel-testing.
- **Verification:** `npm test` 240/240; `npm run build` exit 0.

## D-023 — Inc 5 automated gate: headless checks PASS; interactive + asset items remain human/asset-gated
- **Decision (2026-07-14):** run every Inc-5 gate check that is possible headless and record the result; the rest is explicitly flagged as requiring a human at a browser (Phaser can't render here) or missing binary assets.
- **PASS (automated, this session):**
  - **Build:** `npm run build` exit 0; `nuxt generate` produces `.output/public` (4 routes prerendered).
  - **Tests:** `npm test` 240/240 (23 files) — combat parity, realtime kit behaviour, boss phases, sigils, expeditions, main + side quest reducers/store, save migrations.
  - **No soft-lock (by construction):** dungeon reachability BFS tests (Inc 2); boss phases re-derived from HP; quest chains proven completable end-to-end with no RNG-gated step (tests assert the trigger kinds).
  - **Asset-path / subpath:** grep of the generated output finds **no hardcoded root-absolute game-asset refs**; assets flow through `assetPath()`/`app.baseURL` (`NUXT_APP_BASE_URL` drives the GitHub Pages subpath).
  - **Save-compat (additive):** every new live-store field (`socketedSigils`, `mainQuest`, `sideQuestProgress`, `sideQuestClaimed`) has a `state()` default **and** an `afterHydrate` guard ⇒ a pre-Phase-14 `player` blob loads with zero loss; `SAVE_ENVELOPE_ENABLED` stays `false` (legacy authoritative).
- **REMAINS (cannot close headless):** the interactive fresh-profile playthrough of the whole World-1 slice (flips #2–#7 + quests) on **desktop / mobile / reduced-motion** — per-class kit HUD feel, boss 3-channel telegraph readability, Knowledge-Break flow + summary-persist, sigil socketing, expedition + quest boards, mage MP balance, perf spot-check; plus the asset/scene backlog from D-022. **These are the only things between "code-complete" and a signed Phase-14 gate.**
- **Status:** Phase 14 is **CODE-COMPLETE for the headless-achievable scope**; the gate is **not signed** — it depends on the human playthrough + optional asset polish. Recorded honestly rather than claimed.

## Inc 3 status after flip #7
All seven flag flips landed (#1 `NEW_ZONE_RUNTIME` · #2 `COMBAT_DOMAIN` · #3 `REALTIME_COMBAT` · #4 `KNOWLEDGE_BREAK` · #5 `CLASS_KITS` · #6 `SIGILS` · #7 `ADAPTIVE_EXPEDITIONS`) + the boss 3-phase machine. `SAVE_ENVELOPE_ENABLED` remains `false` by design (§0). **Inc 3 code is complete; its gate closure depends on the outstanding interactive browser playthrough of #2–#7** (cannot run headless). Remaining Phase 14: **Inc 4** (World-1 content — largest drop) + **Inc 5** (qa-release independent gate, human-driven). Per the standing grant, the autonomous flag work stops here at the Inc-3/Inc-4 boundary; Inc 4 is a fresh, large content undertaking and Inc 5 requires human verification.
