# Project Subagents

วางไฟล์ `.md` ที่นี่เพื่อกำหนด subagent เฉพาะโปรเจกต์ (เช่น agent สำหรับตรวจสอบความยาก
ของคำถาม O-NET, agent สำหรับ balance ตัวเลข scaling ของหอคอย)

ดูรูปแบบไฟล์ agent ได้จากเอกสาร Claude Code: แต่ละไฟล์มี frontmatter `name`/`description`/`tools`
ตามด้วยคำสั่งของ agent นั้น
