# ComfyUI Pipeline — A0 Preflight Report

> Generated by the ComfyUI asset-generation Master Setup (Setup Pack §27, phase A0).
> Records machine/health state at install time. No gameplay change; no asset generation.

## Repository state
- **Branch:** `tooling/comfyui-asset-pipeline` (created off `foundation/sgrade-full-transformation`).
- **Working tree:** clean apart from the untracked Setup Pack doc (`RPG_ONET_COMFYUI_ASSET_GENERATION_SETUP_PACK_TH.md`), committed as part of this pipeline.
- **Current gameplay phase:** Phase 13 complete, committed and pushed (`5d935bd`). No gameplay files are touched by this pipeline.

## Machine / device
- **GPU:** NVIDIA GeForce RTX 3060 Ti — **8192 MiB VRAM**, driver 595.79.
- **VRAM guidance (Setup Pack §4):** 8 GB ⇒ concept + medium icon workflows, batch 1. Not sized for multi-stage/high-batch reference workflows.
- **Disk (C:):** ~15 GB free of 195 GB (93% used). ⚠️ **Low.** ComfyUI + model weights must NOT be installed here — use `C:\AI\...` or `D:\AI\...` per Setup Pack §3. This repo lives under OneDrive, which is also unsuitable for ComfyUI/models.
- **Python:** 3.12.10 (`python` / `py`). **Pillow 12.3.0** present — sufficient for validation + contact sheets.

## ComfyUI health
- **`GET http://127.0.0.1:8188/system_stats`:** no response — **ComfyUI is not running.**
- **`GET .../queue`:** no response.
- **Consequence:** the optional local smoke job is **skipped** (Setup Pack §27 → "If no API workflow exists: do not fabricate one; write exact export instructions; finish installation without generation"). No API workflow has been exported yet either.

## Subagent frontmatter support
- Existing `.claude/agents/*.md` use: `name, description, model, effort, permissionMode, maxTurns, isolation, color`. The pack's proposed agent frontmatter is fully supported — no fields removed.

## Blockers / assumptions
- **ComfyUI offline** → no live API smoke this run. Foundation installs fully; generation is deferred until ComfyUI is running and a workflow is exported (Setup Pack §28).
- **No exported API workflow** → `workflow-registry.yaml` ships a placeholder entry (`ui_icon_v1`) marked `status: unregistered` with export instructions; node IDs are intentionally left blank (never guessed).
- **`RPG_ONET_NEW_GAME_FOUNDATION_MASTER_PLAN_TH.md` absent** — consistent with the existing execution-doc assumption (roadmap lives in the execution prompt + `docs/`); not required for A0–A5.
- **Low C: disk** → do not download models to this drive.

## Definition: what A0 guarantees
- No gameplay/source files modified. No models downloaded. No custom nodes installed. ComfyUI not exposed beyond localhost (it isn't running at all).
