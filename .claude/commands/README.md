# Custom Slash Commands

วางไฟล์ `.md` ที่นี่เพื่อสร้าง custom slash command ของโปรเจกต์นี้ (เรียกใช้ด้วย `/ชื่อไฟล์`)

ตัวอย่าง: `add-monster.md` → เรียกด้วย `/add-monster` เพื่อให้ Claude เพิ่มมอนสเตอร์ใหม่ตาม
เทมเพลตใน `templates/` โดยอัตโนมัติ
